import React from 'react'
import { Layout } from '@douyinfe/semi-ui'
import './index.module.less';

const BFooter = () => {
	return (
		<></>
		// <Layout.Footer className='base-footer'>
		// 	FOOTER
		// </Layout.Footer>
	)
}

export default React.memo(BFooter)
